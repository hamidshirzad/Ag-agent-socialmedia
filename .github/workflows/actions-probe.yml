name: Actions probe

# TEMPORARY. Determines whether GitHub Actions can execute in this repository
# at all. Uses no external actions and no checkout, so the only thing it can
# test is whether a hosted runner is assigned and a step is executed.
# Removed in the following commit once the answer is recorded.

on:
  pull_request:

permissions: {}

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Prove a runner executed a step
        run: echo "runner executed"
